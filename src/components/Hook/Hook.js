import React from 'react';
import { HookBanner, HookLabel, HookText } from './HookStyles';
import { Section } from '../../styles/GlobalComponents';

const Hook = () => {
  return (
      <Section align="center">
        <HookBanner>
            <HookText>
              Available now, Senior Rails / Full-Stack roles · Remote in any timezones or hybrid / onsite in Pakistan
            </HookText>
        </HookBanner>
      </Section>
  );
};

export default Hook;
